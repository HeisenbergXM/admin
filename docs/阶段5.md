## 阶段 5 检查结果 — 销售管理：车辆销售

| # | 检查项 | 结果 | 备注 |
|---|--------|------|------|
| V5.1 | 项目编译通过 | ✅ | 已执行 `mvn -q compile`，退出码 0 |
| V5.2 | 创建销售草稿 | ✅ | 单测覆盖创建草稿并写入 `DRAFT` |
| V5.3 | 非 `PENDING_ALLOCATION` 拒绝 | ✅ | 创建时调用 `LifecycleService.assertStage(PENDING_ALLOCATION)` |
| V5.4 | 更新草稿 | ✅ | 已实现 `PUT /api/allocations/{vehicleId}` |
| V5.5 | 确认 | ✅ | 单测覆盖确认后推进到 `PENDING_INVOICE` |
| V5.6 | 重复确认拒绝 | ✅ | 更新/确认复用生命周期锁定校验 |
| V5.7 | 列表查询 | ✅ | 已实现 VIN、经销商、阶段筛选 |

### 自动化验证

- `mvn -q -Dtest=VehAllocationServiceImplTest test`：通过
- `mvn -q compile`：通过
