## 阶段 3 检查结果 — 车辆主记录 + 生产管理

| # | 检查项 | 结果 | 备注 |
|---|--------|------|------|
| V3.1 | 项目编译通过 | ✅ | 已执行 `C:\Users\arthu\software\apache-maven-3.9.16\bin\mvn.cmd -q compile`，退出码 0 |
| V3.2 | 创建车辆草稿 | ✅ | `VehProductionServiceImplTest#createProductionCreatesVehicleAndDraftProduction` 覆盖：写入 `t_vehicle` + `t_veh_production`，阶段为 `PENDING_OFFLINE` / `DRAFT` |
| V3.3 | VIN 唯一性 | ✅ | `VehProductionServiceImplTest#createProductionRejectsDuplicateVin` 覆盖重复 VIN 返回 `VIN_DUPLICATE` |
| V3.4 | VIN 异步查重 | ✅ | 已实现 `GET /api/vehicles/check-vin?vin=xxx`，返回 VIN 是否可用 |
| V3.5 | 更新草稿 | ✅ | 已实现 `PUT /api/production/{vehicleId}`，更新前通过 `LifecycleService.assertNotConfirmed` 锁定确认记录 |
| V3.6 | 确认录入 | ✅ | `VehProductionServiceImplTest#confirmProductionLocksProductionAndAdvancesLifecycle` 覆盖：生产记录确认 + 推进 `PENDING_OFFLINE -> PENDING_INBOUND` |
| V3.7 | 重复确认拒绝 | ✅ | 复用 `LifecycleService.confirmAndAdvance` / `assertNotConfirmed`，返回 `STAGE_ALREADY_CONFIRMED` |
| V3.8 | 确认后不可改 | ✅ | 更新草稿时检查 `stage_status`，已确认记录拒绝修改 |
| V3.9 | 车辆列表查询 | ✅ | 已实现 `GET /api/vehicles`，支持 VIN、车型、车系、阶段、经销商筛选 |
| V3.10 | VIN 候选查询 | ✅ | 已实现 `GET /api/vehicles/candidates?stage=...`，复用 `VehicleMapper.selectCandidates` |

### 自动化验证

- `C:\Users\arthu\software\apache-maven-3.9.16\bin\mvn.cmd -q -Dtest=VehProductionServiceImplTest test`：3 tests, 0 failures, 0 errors, 0 skipped
- `C:\Users\arthu\software\apache-maven-3.9.16\bin\mvn.cmd -q compile`：通过
- `C:\Users\arthu\software\apache-maven-3.9.16\bin\mvn.cmd -q test`：通过

### 新增/修改文件

**车辆通用查询**
- `dto/request/VehicleQueryRequest.java`
- `dto/request/VehicleCandidateRequest.java`
- `dto/response/VehicleListResponse.java`
- `dto/response/VehicleDetailResponse.java`
- `service/VehicleService.java`
- `service/impl/VehicleServiceImpl.java`
- `controller/VehicleController.java`
- `mapper/VehicleMapper.java`（新增分页查询方法）
- `resources/mapper/vlm/VehicleMapper.xml`（新增分页联查 SQL）

**生产管理**
- `dto/request/ProductionSaveRequest.java`
- `dto/response/ProductionResponse.java`
- `entity/VehProduction.java`
- `mapper/VehProductionMapper.java`
- `service/VehProductionService.java`
- `service/impl/VehProductionServiceImpl.java`
- `controller/VehProductionController.java`

**测试**
- `src/test/java/com/company/admin/service/impl/VehProductionServiceImplTest.java`

### 人工验收建议

1. 启动应用后访问 `http://localhost:8899/doc.html`，确认「车辆通用查询」「生产录入」两个 Tag 可见。
2. 通过 Knife4j 依次验证：创建草稿、重复 VIN、查重、更新草稿、确认录入、重复确认、确认后更新拒绝。
3. 在 MySQL 中检查 `t_vehicle.lifecycle_stage` 和 `t_veh_production.stage_status/confirmed_by/confirmed_at` 是否符合预期。

**检查结论**：阶段 3 后端代码、单元测试、编译验证已完成；按执行文档，建议人工完成接口/数据库验收后再进入阶段 4。
