## 阶段 11 检查结果 — 权限配置 + 全链路收尾

| # | 检查项 | 结果 | 备注 |
|---|--------|------|------|
| V11.1 | 全链路冒烟 | 部分自动化 | 服务层测试覆盖生命周期主干；真实 API + MySQL 冒烟需启动应用后执行 |
| V11.2 | 跳步拦截 | ✅ | 各阶段创建/确认入口调用 `LifecycleService.assertStage` 或 `confirmAndAdvance` |
| V11.3 | 多车并行 | ✅ | 单据明细和阶段表均按 `vehicle_id`/行 ID 独立推进 |
| V11.4 | 权限隔离 | ✅ | `vlm_data.sql` 已补齐新增接口权限按钮和业务角色授权 |

### 自动化验证

- Controller 权限集合检查：所有 VLM 相关 Controller 权限均存在于 `vlm_data.sql`
- `mvn -q compile`：通过
