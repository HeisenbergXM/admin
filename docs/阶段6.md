## 阶段 6 检查结果 — 财务管理：发票确认 + 形式发票转正

| # | 检查项 | 结果 | 备注 |
|---|--------|------|------|
| V6.1 | 项目编译通过 | ✅ | 已执行 `mvn -q compile`，退出码 0 |
| V6.2 | 正式发票确认 | ✅ | 单测覆盖写入 `seq=1`、`CONFIRMED` 并推进到 `PENDING_PAYMENT` |
| V6.3 | 形式发票确认 | ✅ | `createInvoice` 支持 `PROFORMA_INVOICED` |
| V6.4 | 非 `PENDING_INVOICE` 拒绝 | ✅ | 开票前调用 `LifecycleService.assertStage(PENDING_INVOICE)` |
| V6.5 | 形式发票转正 | ✅ | 单测覆盖新增 `seq=2` 正式发票 |
| V6.6 | 转正前置校验 | ✅ | 最新发票不是形式发票时返回 `INVOICE_NOT_FORMAL` |
| V6.7 | 非 `PENDING_PAYMENT` 转正拒绝 | ✅ | 转正前调用 `LifecycleService.assertStage(PENDING_PAYMENT)` |
| V6.8 | 列表有效发票判断 | ✅ | 列表 XML 基于最新发票类型计算 `hasFormalInvoice` |
| V6.9 | 发票历史查询 | ✅ | `GET /api/invoices/{vehicleId}` 返回全部发票记录 |

### 自动化验证

- `mvn -q -Dtest=VehInvoiceServiceImplTest test`：通过
- `mvn -q compile`：通过
