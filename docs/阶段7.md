## 阶段 7 检查结果 — 财务管理：收款确认

| # | 检查项 | 结果 | 备注 |
|---|--------|------|------|
| V7.1 | 项目编译通过 | ✅ | 已执行 `mvn -q compile`，退出码 0 |
| V7.2 | 正式发票车辆收款 | ✅ | 单测覆盖确认收款推进到 `PENDING_DELIVERY` |
| V7.3 | 形式发票未转正拒绝收款 | ✅ | 确认前调用 `LifecycleService.hasValidFormalInvoice` |
| V7.4 | 形式发票转正后可收款 | ✅ | 有效正式发票判断集中在 `LifecycleService` |
| V7.5 | 非 `PENDING_PAYMENT` 拒绝 | ✅ | 创建草稿前调用阶段校验 |
| V7.6 | 确认后锁定 | ✅ | 更新草稿调用 `assertNotConfirmed` |
| V7.7 | 列表查询 | ✅ | 已实现 VIN、收款状态、阶段、正式发票筛选 |

### 自动化验证

- `mvn -q -Dtest=VehPaymentServiceImplTest test`：通过
- `mvn -q compile`：通过
