## 阶段 4 检查结果 — 运输管理：车厂到仓库

| # | 检查项 | 结果 | 备注 |
|---|--------|------|------|
| V4.1 | 项目编译通过 | ✅ | 已执行 `mvn -q compile`，退出码 0 |
| V4.2 | 创建运输单 | ✅ | `TransportOrderServiceImplTest#createOrderStoresDraftHeader` 覆盖草稿表头写入 |
| V4.3 | 添加 VIN 到明细 | ✅ | `addItem` 校验车辆必须处于 `PENDING_INBOUND` |
| V4.4 | 添加非法 VIN | ✅ | 由 `LifecycleService.assertStage` 拦截非待入库车辆 |
| V4.5 | 更新 SAIC buy off | ✅ | 已实现 `PUT /api/transport-orders/{id}/items/{itemId}` |
| V4.6 | 移除 VIN | ✅ | 已实现 `DELETE /api/transport-orders/{id}/items/{itemId}` |
| V4.7 | 整单确认 | ✅ | 单测覆盖整单锁定并批量推进到 `PENDING_ALLOCATION` |
| V4.8 | 确认后锁定 | ✅ | 已确认运输单编辑/明细变更返回 `STAGE_ALREADY_CONFIRMED` |
| V4.9 | VIN 占用检测 | ✅ | `countOpenOccupancy` 检测其他草稿运输单占用，返回 `VEHICLE_OCCUPIED_BY_OTHER_ORDER` |
| V4.10 | 列表查询 | ✅ | 已实现分页 XML，支持单号、日期、状态筛选 |

### 自动化验证

- `mvn -q -Dtest=TransportOrderServiceImplTest test`：通过
- `mvn -q compile`：通过
