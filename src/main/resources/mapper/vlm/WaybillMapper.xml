<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.company.admin.mapper.WaybillMapper">

    <select id="selectByDispatchListId" resultType="com.company.admin.entity.Waybill">
        SELECT id, waybill_no, dispatch_list_id, trolly_type, fully_load
        FROM t_waybill
        WHERE dispatch_list_id = #{dispatchListId}
        ORDER BY id ASC
    </select>
</mapper>
