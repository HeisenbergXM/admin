<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.company.admin.mapper.WaybillDealerVinMapper">

    <select id="countOpenVin" resultType="java.lang.Long">
        SELECT COUNT(1)
        FROM t_waybill_dealer_vin v
        INNER JOIN t_waybill_dealer d ON d.id = v.waybill_dealer_id
        WHERE v.vehicle_id = #{vehicleId}
          AND d.row_status = 'DRAFT'
          AND v.waybill_dealer_id != #{dealerRowId}
    </select>

    <select id="selectByDealerRowId" resultType="com.company.admin.entity.WaybillDealerVin">
        SELECT id, waybill_dealer_id, vehicle_id
        FROM t_waybill_dealer_vin
        WHERE waybill_dealer_id = #{dealerRowId}
        ORDER BY id ASC
    </select>
</mapper>
